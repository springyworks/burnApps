use fft_gpu::run;

fn main() {
    // This example just calls the main run function, effectively running the app.
    // You can pass arguments like "--on_cpu" or "--generate-video" to this example.
    run();
}
